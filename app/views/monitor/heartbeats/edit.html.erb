<% content_for :title, "Editing heartbeat" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing heartbeat monitor</h1>

  <%= turbo_frame_tag dom_id(@monitor_heartbeat) do %>
    <%= render "form", monitor_heartbeat: @monitor_heartbeat %>
  <% end %>

  <%= link_to "Back to monitors", root_path, class: "w-full sm:w-auto text-center mt-2 sm:mt-0 sm:ml-2 rounded-md px-3.5 py-2.5 bg-gray-100 hover:bg-gray-50 inline-block font-medium" %>
</div>
