<%= turbo_stream_from heartbeat %>

<div id="<%= dom_id heartbeat %>" class="w-full sm:w-auto my-5 space-y-5">
  <%= heartbeat.name %>
  <%= render "reports/minimal", monitor: heartbeat, reports: heartbeat.reports.select(:id, :up) %>
</div>
