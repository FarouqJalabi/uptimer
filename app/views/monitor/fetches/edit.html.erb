<% content_for :title, "Editing fetch" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing fetch monitor</h1>

  <%= turbo_frame_tag dom_id(@monitor_fetch) do %>
    <%= render "form", monitor_fetch: @monitor_fetch %>
  <% end %>

  <%= link_to "Back to monitors", monitors_path, class: "btn mt-2" %>
</div>
